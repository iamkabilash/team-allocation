import React, { useState } from "react";

const GroupedTeams = ({employees, selectTeam}) =>{
    return (
        <div className="flex flex-col items-center font-bold mt-[40px] mb-[20px]">
            <h1>Grouped teams</h1>
        </div>
    );
}

export default GroupedTeams;